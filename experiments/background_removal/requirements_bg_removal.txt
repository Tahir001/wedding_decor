# Wedding Decor — Production-Grade Item Extraction Pipeline
# Two approaches: SAM2 + alpha matting (A) and BiRefNet/RMBG-2.0 (B)

torch>=2.1.0
torchvision>=0.16.0
transformers>=4.44.0
accelerate>=0.25.0
pillow>=10.0.0
scipy>=1.11.0
numpy>=1.24.0

# Visualization
supervision>=0.22.0
opencv-python-headless>=4.8.0
matplotlib>=3.7.0

# Alpha matting (Script A: SAM2 + PyMatting)
pymatting>=1.1.0

# Segmentation backends:
# SAM2 native (Script A): pip install git+https://github.com/facebookresearch/sam2.git
# RMBG-2.0 / BiRefNet (Script B): loaded via transformers, no extra install needed

# RunPod quick-install:
#   pip install pymatting
#   pip install git+https://github.com/facebookresearch/sam2.git  (if not already)
#   Models auto-download from HuggingFace on first run
