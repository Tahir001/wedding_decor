# Wedding Decor — Item Extraction Pipeline
# No diffusion models — detection + segmentation + visualization only

torch>=2.1.0
torchvision>=0.16.0
transformers>=4.44.0
accelerate>=0.25.0
pillow>=10.0.0
scipy>=1.11.0
numpy>=1.24.0

# Visualization (from Roboflow Grounded-SAM pattern)
supervision>=0.22.0
opencv-python-headless>=4.8.0
matplotlib>=3.7.0

# Segmentation backends (install at least one):
# SAM2 native (best quality): pip install git+https://github.com/facebookresearch/sam2.git
# FastSAM (10-20x faster):    pip install git+https://github.com/CASIA-IVA-Lab/FastSAM.git
#                              + wget FastSAM-x.pt weights
# If neither installed, SAM2 falls back to transformers backend